# RINGFALL — DEFECT LEDGER

Append-only. Every agent may add. Nobody may delete an entry; entries are closed, not removed.

**Severity:** `frame-ruining` / `major` / `minor`
**Every entry must carry a measurement.** Entries without one are closed as `discarded-unmeasured`.

## Open compensations

Any constant marked `// COMPENSATION:` in source must have a row here and is counted as an open
defect until the upstream cause is fixed and the constant removed.

| ID | File:line | Compensating for | Measurement | Opened | Status |
| --- | --- | --- | --- | --- | --- |
| VEH1-C | `src/vehicles/index.js:370` | `physics.raycast`/`rayObb` returning a wrong (side-face) hit normal for a subset of straight-down suspension rays (see `DEFECTS.md` "VEH1" below). Suspension force is applied without any slope-based de-rating as a result — see `src/vehicles/README.md` "Known limitations". | Repro isolated directly against `PhysicsWorld.raycast`/`rayObb`, independent of vehicle code: a static box `addStaticBox(center=(0,-1,0), halfExtents=(50,1,50))`, ray straight down (`dir=(0,-1,0)`) from `(x, 0.45, 0)` returns the correct top-face normal `(0,1,0)` for `|x| ≤ ~0.99`, then flips to a side-face normal `(1,0,0)` (or `(0,0,±1)` when the offset is on Z) for `|x| ≥ 1.0`, regardless of box size (reproduced at half-extents 5, 10, 20, 50, 300) and regardless of how far the hit point is from the box edge (the hit *point* itself stays correct — on the true top face — only the reported *normal* is wrong). All four Ridgeback wheel mounts sit at `|x| ≈ 1.175` / `|z| ≈ 1.7` from the vehicle centre, so every wheel hit this. Before the workaround, this made every wheel's suspension force ~30% of correct (via a `clamp01(normalY+0.3)` de-rate the vehicle code used to apply) and the vehicle sank through the floor — see `src/vehicles/selftest.js` "rest-equilibrium" case, which now passes. | 2026-07-25 | open |
| AI4-C | `src/ai/agent.js` — `AiAgent.onExternalDamageApplied()` | `src/weapons/index.js:_damageTarget()` writing `state.shield`/`state.health`/`state.dead` directly, bypassing the `CULL` barrier and `WARDEN` plate mitigation that only `src/ai` can compute (see AI4 below). | Front-arc `Vector BR` on a `CULL` through the real `WeaponSystem`: without the compensation health went 55 -> 0 with `entity.killed`, where FEEL.md F48 requires 0 damage. With it, health stays 55 until the barrier's 90 is absorbed, then takes full damage (measured 47 after the 13th round). | 2026-07-25 | open |

## Open defects

| ID | Round | Sev | Subsystem | Description | Measurement | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ART1 | 1 | major | lighting / gate design | ART.md P2 (global contrast 8:1–20:1) fails on the whole-set median. Diagnosis: the 12-shot set mixes frames that contain architecture with frames that are open sky over flat grass, and the latter are low-contrast **by content**, not by lighting. Frames with real architecture are inside or above the window; sky/grass frames cannot reach 8:1 no matter how the scene is lit. Either P2 must be measured on a content-qualified subset, or the window is wrong for this art direction. Not fixed by distorting the image to hit a number written in Phase 0 before anything was measured. | Median across all 12 shots 5.72 (was 4.65 before the fog-onset fix). Per shot: enemy_15m 26.7, shield_flare 18.9, firefight 16.0, int_corridor 6.7, ext_vista 6.1, night_emissive 6.1, weapon_close 5.4, int_hall_emissive 5.3, vehicle_ext 4.3, enemy_60m_sil 3.1, ext_ground 2.6, sky_only 2.6. | lighting | open |
| ART2 | 1 | minor | tooling / palette.mjs | P10's high-frequency measure is a box-blur residual proxy, not a radially-averaged 2D FFT as ART.md §9 specifies. It cannot separate surface high-frequency detail from legitimate geometric silhouette edges, so a darker, higher-contrast interior scores *worse* on it despite no material change. | int_corridor went 0.079 → 0.115 across a change that only altered fog onset and touched no surface pattern. int_hall_emissive 0.024, weapon_close 0.012, enemy_15m 0.055 all pass. | orchestrator | open |
| ART3 | 1 | minor | materials / ART.md §3.3 | A violet emissive cannot hold HSV S ≥ 0.25 (ART.md P4) above a post-exposure luminance of 1.22, because a hue whose maximum-luminance form is dark desaturates on clamp when driven toward display white. This is a display-gamut property, not a tonemap defect — lowering DESAT_STRENGTH from 0.22 to 0.08 moved it only 0.195 → 0.226. ART.md §3.3 permits emissive up to 8× adjacent diffuse (≈2.7 post-exposure), which would breach it. Shipped violet runs at L=0.40 and passes at S=0.496, so no content violates it today; the ceiling needs recording in ART.md so a future art pass does not walk into it. | Crossover luminance by hue: cyan never, orange never, yellow never, mint 3.54, violet 1.22. Shipped values all pass: cyan 0.612, violet 0.496, orange 0.859, yellow 0.778, mint 0.358. | materials | open |
| UI1 | 0 | minor | ui (addressed to orchestrator) | `shield.broken` / `shield.recharge.start` / `shield.recharge.full` (ARCHITECTURE.md §4.1) carry `entityId` and fire for every shielded entity, including a `VANE` (90 shield). Nothing in the brief's `hudState` shape or event vocabulary tells `src/ui` which `entityId` is the local player, so `Hud`'s screen-edge shield-break flare would fire on enemy shield pops too unless filtered. Mitigated locally with an opt-in `hud.setPlayerId(id)` (permissive/unfiltered until called) — see `src/ui/README.md` "Integration contract". Needs a real decision: either the orchestrator calls `hud.setPlayerId()` once the player entity spawns, or the event payloads gain an `isLocalPlayer`/similar flag upstream. | Reviewed ARCHITECTURE.md §4.1, §5.6, §5.7 and `src/shared/*` — confirmed no player-id convention exists anywhere `ui` is permitted to read. | orchestrator | open |
| AUDIO1 | 0 | minor | audio (addressed to orchestrator) | `player.footstep`, `player.landed`, `player.jumped`, `weapon.dryfire`, `weapon.reload.start/end`, `weapon.swap.start/end`, `weapon.overheat`, `weapon.cooled`, `weapon.charge.start/full/released`, `scope.enter/exit/descoped`, `melee.swing`, `ai.vocalize`, and `grenade.stuck` (ARCHITECTURE.md §4.2–4.5) carry no world `point`/`position` field, so `audio` cannot spatialize these with a `PannerNode` — they play dry/centered on the listener regardless of which entity emitted them. For the local player this is usually fine (most of these are first-person events anyway), but it means an AI's `melee.swing` whoosh or `ai.vocalize` chatter plays at full, unpositioned volume no matter how far away it is, which is wrong for readability at range. `vehicle.impact`/`.rolled`/`.flipped` have the same gap; worked around locally by caching each vehicle's last known position from `setVehicleEngine`. Recommend adding a `point[3]`/`position[3]` to whichever of these are emitted by non-player entities, at minimum `melee.swing`, `ai.vocalize`, and the three vehicle events. | Diffed every event payload in ARCHITECTURE.md §4 against the fields `audio` needs for `createSpatialChain`: 14 of the audio-relevant payloads have no position field. | orchestrator | open |
| AUDIO2 | 0 | minor | audio (addressed to orchestrator) | `grenade.thrown` (ARCHITECTURE.md §4.3) has no `grenadeId`, while `grenade.bounced`, `grenade.stuck`, and `grenade.detonated` all do. `audio`'s proximity-tick tracker (`src/audio/grenadeTracker.js`) needs `.thrown` to seed a ballistic estimate and would ideally reconcile it against later bounce/detonate ground truth by id, but can't — it currently free-falls an anonymous tracked point under `GRAVITY` and expires it by its own fuse timer instead. Works for the common 1–2-grenades-in-flight case; ambiguous with several grenades in flight simultaneously. Recommend adding `grenadeId` to `grenade.thrown`'s payload. | `grenade.thrown` payload is `{ownerId, grenadeType, origin, velocity, fuseMs}` — no `grenadeId` field, confirmed against ARCHITECTURE.md §4.3 and `src/shared/events.js`. | orchestrator | open |

| AI1 | 0 | minor | ai (addressed to orchestrator) | `src/shared/tuning.js` carries every graded number for the enemies (`ENEMY.*`, `AI.*`) but not the behaviour-shaping numbers the AI needs to actually run: per-archetype fire cadence (burst count, intra-burst interval, refire, magazine, reload), preferred/standoff ranges, aim and yaw slew rates, `BARRIER_TRACK_RATE`, lose-target and seek timeouts, avoidance lookahead, and cover-sampling density. These are declared in `src/ai/constants.js` with an explicit header saying they are AI-local and duplicate nothing in `tuning.js`. None is currently a `FEEL.md` §8 graded assertion, so this is not a violation today — but if any of them ever becomes graded (a plausible candidate: the CULL sidestep period is already half-specified, `ENEMY.CULL.strafePeriod` lives in `tuning.js` while the strafe *magnitude* lives in `src/ai`), it must be promoted into `tuning.js`, which only the orchestrator may write. | `src/ai/constants.js` has 43 exports: 2 state enums (`AiState`, `AnimState`), 4 values *derived* from `AI.*` (`HALF_FOV_COS`, `SIGHT_RANGE_SQ`, `HEARING_RADIUS_SQ`, `SQUAD_ALERT_RADIUS_SQ`), 1 pure re-export of `AI.missGraceOnAcquire`, 1 per-archetype behaviour table (`ARCHETYPE_AI`, 4 archetypes × 8–13 fields), and **35 genuinely new scalar/array constants** with no `tuning.js` home. Name-collision check between the two files' export lists (`comm -12` over sorted `export const` names): **0 collisions**, i.e. nothing is re-declared. | orchestrator | open |
| AI2 | 0 | minor | ai (addressed to orchestrator) | `damage.applied` (ARCHITECTURE.md §4.1) carries `point[3]` and `normal[3]` but no attacker world position. Two FEEL.md §6 behaviours are **directional** and need the bearing from target to attacker: the `CULL` forward arm barrier (F48, 62° frontal arc) and the `WARDEN` front plates (×0.35 `KINETIC` frontal, back plate is the weakpoint). `src/ai` currently infers the bearing in priority order `sourcePosition` (a non-standard field it accepts on its own direct `applyDamage()` entry point) → `normal` → `point − center` → last-known-target → own forward. Using `normal` is only correct when the surface normal genuinely faces the shooter, and `point − center` is wrong for any explosion or grenade whose hit point is not on the line to the attacker. Recommend adding `sourcePosition[3]` (or reusing `point[3]` with a documented meaning of "attacker-side impact point") to the `damage.applied` payload. | Measured against a `CULL` facing a threat at +Z, shot from behind (−Z), 30 raw damage, 4 cases: **(A)** `KINETIC`, only `point[3]` supplied and set to the model-centre hit position → **0 damage taken** (degenerate zero bearing falls through to the last-known-target fallback, i.e. the front arc; correct answer 30). **(B)** `KINETIC`, `point[3]` on the model's back surface → **30** (correct). **(C)** `KINETIC` with `sourcePosition` → **30** (correct; this is the self-test row "F48 CULL barrier does NOT protect from behind"). **(D)** `PLASMA`, only `point[3]` = model centre → **5.775** where the unmitigated answer is 16.5. So the current payload is correct only when the emitter happens to put `point[3]` on the attacker-facing surface, and silently wrong for a model-centre hit point or any blast whose centre is not on the line to the attacker. | orchestrator | open |
| VEH1 | 0 | major | physics (addressed to physics owner) | `PhysicsWorld.raycast` (and, by inspection of the shared code path, `sphereCast`/`_sphereCastStatics`) returns the **wrong hit normal** for a ray against a `StaticBox` once the ray's origin is offset more than roughly 1.0 units (in world space, independent of box size) from the box's centre in the axis perpendicular to the ray. A straight-down ray that geometrically strikes the box's top face (the hit **point** is correct, exactly on the top face, at the correct distance) reports a **side-face normal** instead of the top-face normal. This silently breaks any consumer that reads `hit.normal` for slope/surface-orientation logic off-centre from a static box — `vehicles`' suspension code was the first thing to hit it (every one of the Ridgeback's 4 wheel mounts is offset ~1.2-1.7 units from a level ground box's centre) and worked around it by not using `hit.normal` for suspension slope de-rating at all (see the `// COMPENSATION:` at `src/vehicles/index.js:370` and the matching "VEH1-C" row above). Likely root cause: `rayObb`'s face-classification step in `src/physics/geometry.js` picking the wrong axis/face when comparing exit-distances near a value that happens to coincide with an absolute (not box-relative) magnitude around 1.0 — needs the `physics` owner's own investigation since this agent does not have write access to `src/physics/**`. | Isolated, minimal repro (no vehicle code involved): `world.addStaticBox(new Vector3(0,-1,0), new Vector3(50,1,50), new Quaternion(), SurfaceId.CONCRETE, 0)`; `world.raycast(new Vector3(x, 0.45, 0), new Vector3(0,-1,0), 0.5, null)`. For `x` in `{0, 0.01, 0.05, 0.1, 0.2, 0.3, 0.5, 0.6, 0.7, 0.8, 0.9, 0.95, 0.99}`: `hit.normal = (0,1,0)` (correct) every time, `hit.distance = 0.45` every time. At `x = 1.0` and above (tested up to `x=2`, and separately at the Ridgeback's actual wheel offsets `(-1.175, 0.45, -1.7)` etc.): `hit.normal` flips to `(1,0,0)` (or a Z-axis normal when the offset is on Z), while `hit.distance` stays exactly `0.45` and `hit.point` stays exactly on the top face (`y ≈ 0`). Reproduced across box half-extents `{5,10,20,50,300}` — same threshold, ruling out box-size dependence. Reproduced with a fresh `PhysicsWorld` per call, ruling out shared-hit-record aliasing across calls. | 2026-07-25 | open |
| VEH2 | 0 | minor | vehicles (addressed to orchestrator) | The task brief asks the Ridgeback's turret and ram-damage pipelines to emit `damage.applied` via `resolveDamage()`, and separately asks vehicles to "register the vehicle as a target so it can be shot and stuck with plasma grenades" via `../weapons/targets.js`. Neither is fully wireable today: (1) `src/weapons/targets.js` does not exist (`src/weapons/` itself doesn't exist yet), so there is nothing to import/register against; (2) there is no shared entity/hitbox registry anywhere in the codebase (`src/shared/*`, `src/core/*`) that would let a headless L5 system turn a raycast/sphereCast `entityId` into a mutable `{shield, health}` state to hand to `resolveDamage()` — `physics.raycast`/`sphereCast` are static-geometry-only per `src/physics/README.md`, so they only ever return a non-zero `entityId` if some other system has registered a moving entity's hitbox as a static shape and keeps it updated, which nothing does yet. `VehicleSystem`/`Vehicle` accept an optional, additive `resolveDamageTarget(entityId) -> {shield,health}|null` constructor field as the intended integration point (the literal brief signature `new VehicleSystem({physics, events})` still works unmodified with it omitted), and `Vehicle.applyDamage(amount, damageType, hitRegion)` is added as a public method beyond the brief's listed API for a future weapons/AI pipeline to call. Both are exercised and pass in `src/vehicles/selftest.js` (F45a/F45b) using a test-local stub registry, proving the mechanism works — it just has nothing real plugged into it yet. Recommend: once `weapons`/`ai`/`characters` exist, either (a) add a shared entity-hitbox registry the orchestrator owns under `src/core/` or `src/shared/`, or (b) have the owner of entity health state pass a `resolveDamageTarget` callback into `VehicleSystem`'s constructor at wiring time in `src/game/index.js`. | Grepped the whole repo for an entity/hitbox registry pattern (`ls src/weapons`, `ls src/characters`, `ls src/ai` — none exist yet) and reviewed `src/physics/README.md`'s "Known limitations" (static-geometry-only queries, no dynamic-vs-dynamic collision). Confirmed `src/vehicles/selftest.js` F45a/F45b pass using a local `Map`-backed stub for `resolveDamageTarget`, proving the call path (raycast/sphereCast hit → `resolveDamageTarget` → `resolveDamage()` → `damage.applied`) is correct end-to-end once a real registry exists. | orchestrator | open |
| AI3 | 0 | minor | ai (addressed to orchestrator) | There is no frozen event for the `CULL` arm barrier breaking. FEEL.md §6 specifies the barrier "blocks `KINETIC` entirely from the front (0 damage, **ricochet FX**)" and "breaks after 90 absorbed damage" — both are visual/audible moments `fx` and `audio` need. `shield.broken` is wrong for it (the `CULL` has `shield: 0` in `ARCHITECTURE.md` §5.6 and `ENEMY.CULL`, and `src/ui` already flares the screen edge on `shield.broken` — see UI1 — so re-using it would fire a player-shield-break flare on a `CULL` barrier pop). `src/ai` currently signals it as `ai.vocalize {cue:'barrierBroken'}`, but ARCHITECTURE.md §4.5 marks `ai.vocalize` **`audio` only**, so `fx` is not permitted to consume it and the ricochet/shatter cannot be driven at all. Recommend a frozen `ai.barrier.broken {entityId, point[3], normal[3]}` (or a generalised `armor.broken`), plus a per-hit signal for the ricochet — `surface.impact` with `surfaceId: ENERGY_BARRIER` would cover the latter if the weapons owner emits it on a blocked hit. | Barrier state is fully exposed and measured (`barrierHealth` 90 → 60 after 30 blocked `KINETIC`; breaks at exactly 90 absorbed; post-break `KINETIC` deals full 20 — self-test rows F48). Cross-checked all 54 frozen names in `src/shared/events.js`: none covers a non-shield armour piece breaking, and the only barrier-adjacent signal `src/ai` can legally emit is `ai.vocalize`, which ARCHITECTURE.md §4.5 restricts to `audio`. | orchestrator | open |
| AI4 | 1 | major | ai + sandbox (addressed to orchestrator) | **Two owners both implement the damage resolver, and the collision silently breaks FEEL.md F48.** `src/weapons/index.js:_damageTarget()` calls `resolveDamage()`, writes `state.shield`/`state.health` straight into the registered target record, sets `state.dead`, and emits `damage.applied` / `shield.broken` / `health.changed` / `entity.killed`. `src/ai` was briefed to do exactly the same for its own entities (subscribe to `damage.applied`, use `resolveDamage()`, emit the shield/health/killed events) — and it must, because the `CULL` forward arm barrier and the `WARDEN` front plates are **directional archetype mitigation that only `src/ai` can compute** (only it knows `barrierYaw`, `barrierHealth`, `barrierArcDeg`). The weapon system's direct write bypasses that mitigation entirely, and it decides `res.killed` before `src/ai` ever sees the event. Worked around inside `src/ai` with a `// COMPENSATION:` in `AiAgent.onExternalDamageApplied()` (restore this module's authoritative shield/health snapshot, then re-resolve the raw amount with mitigation) plus a `dead` accessor over `alive`. That makes the final STATE correct but cannot suppress the weapon system's already-emitted events, so `entity.killed` still double-fires. Needs a single-resolver decision: either (a) `TargetRegistry` gains a pre-resolution `mitigate(amount, damageType, hitRegion, fromDirection)` hook the weapon system consults before `resolveDamage()`, and `src/ai` stops resolving; or (b) `src/weapons` stops writing state and only emits `damage.applied`, leaving each owner to resolve its own entities. | Real `WeaponSystem` + real `AiDirector` wired through the real `TargetRegistry`, player firing `Vector BR` (KINETIC 8.0/round) into a `CULL`'s **front** arc at 15 m. **Before the compensation:** CULL health **55 -> 0** and `entity.killed` fired, where F48 requires **0 damage** from front KINETIC; the agent was left inconsistent at `alive === true` while `dead === true` (weapons set `state.dead`, which `src/ai` did not mirror), so `TargetRegistry.isValidTarget()` kept returning true for a corpse. **After the compensation:** 13 hits = 104 raw KINETIC -> barrier absorbs rounds 1-12 (96 raw, breaking on the round that crosses 90), health untouched at **55** throughout, round 13 deals the full **8** -> health **47**, barrier **0**, `alive === false`/`dead === true` consistent. Residual: over a 5 s burst, `entity.killed` was observed **2x** (once from `src/weapons`, once from `src/ai`) and `damage.applied` is emitted by weapons with pre-mitigation `amount`. | orchestrator | open |
| FX1 | 0 | minor | fx (addressed to orchestrator) | `vehicle.impact` (ARCHITECTURE.md §4.6) is `{vehicleId, otherId, relativeSpeed, normal[3]}` — a normal but no `point[3]`, and no vehicle position anywhere in the payload. `fx` cannot place the impact sparks/smoke at all. Worked around by caching each entity's last known position from `entity.spawned`, `damage.applied`, `melee.landed` and `vehicle.destroyed`; when a vehicle has produced none of those, the effect is silently dropped and `fx.stats.skippedNoPosition` increments. A vehicle ramming a wall at 18 m/s can therefore produce zero visual feedback. Recommend adding `point[3]` to `vehicle.impact`. Related to AUDIO1, but `audio` can work around it via its `setVehicleEngine` API and `fx` has no equivalent channel. | Diffed `vehicle.impact` against what `fx` needs to spawn: 1 of 4 required fields (`normal`) present, position absent. Confirmed against ARCHITECTURE.md §4.6 and `src/shared/events.js`. Reproduced in `src/fx/selftest.js` — `skippedNoPosition` is non-zero whenever the vehicle was never spawned into the cache. | orchestrator | open |
| FX2 | 0 | minor | fx (addressed to orchestrator) | `weapon.overheat` (ARCHITECTURE.md §4.2) is `{ownerId, weaponId}` with no position, but FEEL.md §4.5 requires a vent plume at the weapon for the full 3.4 s lockout. `fx` anchors the plume to that owner's last `weapon.fired` muzzle; an overheat with no preceding `weapon.fired` from the same `ownerId` produces nothing. Recommend adding `muzzle[3]` (and ideally `direction[3]`, so the plume vents the right way) to `weapon.overheat` and `weapon.cooled`. | `weapon.overheat` payload has 2 fields, neither positional — confirmed against ARCHITECTURE.md §4.2 and `src/shared/events.js`. The `Ember Repeater` is the only weapon that overheats (FEEL.md §4.5), so this affects exactly one but very visible effect. | orchestrator | open |
| FX3 | 0 | minor | fx (addressed to lighting/S1) | ARCHITECTURE.md §6 pass 8 specifies "soft-particle depth fade", but `fx` cannot implement it. (a) `GlobalUniforms` (`src/render/globals.js`) exposes no depth-texture uniform — there is `uAOTex`, `uShadowAtlas`, `uLightsTex`, `uClusterTex`, `uLightIndexTex`, and no `uDepthTex`. (b) More fundamentally, `renderer.hdr` is created with `depthTexture: this.prepass.depthTex` (`src/render/index.js` `setSize`), so during pass 8 the prepass depth texture IS the bound framebuffer's depth attachment; sampling it is a framebuffer feedback loop and WebGL2 will fault the draw rather than return stale texels. Soft particles need either a depth copy taken after pass 5 or a second depth target, plus a `uDepthTex` + `uNearFar` pair in the global uniform block. Both are `src/render`, which `fx` may not write. Shipped without soft fade: particles are hard depth-tested with depth write off, so occlusion is correct but large smoke/dust puffs meet geometry with a hard edge. | Read `src/render/globals.js` (no depth uniform declared, 20 uniforms checked) and `src/render/index.js` `setSize()` (`makeTarget(w, h, { depthTexture: this.prepass.depthTex })`) plus `render()` pass 8 (`r.setRenderTarget(this.hdr)` with no depth rebind). The feedback loop is structural, not a tuning problem. | lighting (S1) | open |
| FX4 | 0 | minor | fx (addressed to orchestrator) | `src/core/rng.js` allocates on every draw. `nextUint()` returns `(Math.imul(s1,5) >>> 0) + (s0 >>> 0)` and `next()` divides that by 2^32; both escape V8's small-integer range, so each call boxes a HeapNumber. This is invisible for a few dozen draws per tick but `fx` draws ~65 values for a single concrete bullet impact. Worked around entirely inside `src/fx` (`src/fx/rand.js` draws `rng('fx')` once at construction into a fixed 8192-entry `Float32Array` and serves the hot path from unboxed typed-array reads — same seeded stream, same determinism, at the cost of periodicity after 8192 draws). Flagging it because `weapons.spread`, `ai`, and `world` generation all use the same API and will pay the same cost. A `nextFloatInto(Float32Array, i)` or a table-backed variant in `src/core/rng.js` would fix it once for everyone. | node 24, `--expose-gc`, 20 000 iterations, heapUsed delta: `rng.next()` measures ~4.5 bytes/call of transient garbage; a single `surface.impact` on `CONCRETE` measured 862 B before the workaround and ~177 B after (the ~177 B remainder is V8 argument boxing on the 23-argument `spawn()`, not the RNG). `update()` steady state measures ~89 B/frame. | orchestrator | open |

## Closed defects

| ID | Closed in round | How it was fixed | Verifying measurement |
| --- | --- | --- | --- |
| — | — | — | — |

## Round counts

| Round | frame-ruining | major | minor | total | Δ vs prev | Verdict |
| --- | --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — | — |
